_format_version: "3.0"

services:
  - name: web
    url: http://web:3000
    routes:
      - name: nextjs-route
        paths:
          - /
        strip_path: false
        preserve_host: true

  - name: api
    url: http://user-api:8000
    routes:
      - name: auth
        paths:
          - /api/v1/auth/login/
          - /api/v1/auth/register/
          - /api/v1/auth/verify/
          - /api/v1/auth/resend-verification/
        strip_path: false
        preserve_host: true
        protocols:
          - http
        plugins:
          - name: rate-limiting
            config:
              minute: 10
              hour: 40
              policy: local
              limit_by: ip
              hide_client_headers: false

      - name: users
        paths:
          - /api/v1/users/me/password/
          - /api/v1/users/me/email/
          - /api/v1/users/me/username/
        strip_path: false
        preserve_host: true
        protocols:
          - http
        plugins:
          - name: rate-limiting
            config:
              minute: 5
              hour: 30
              policy: local
              limit_by: ip
              hide_client_headers: false

      - name: general
        paths:
          - /api/v1
        strip_path: false
        preserve_host: true
        protocols:
          - http
        plugins:
          - name: rate-limiting
            config:
              minute: 50
              policy: local
              limit_by: ip
              hide_client_headers: false
    plugins:
      - name: cors
        config:
          origins:
            - https://dvi-app.xyz
            - https://www.dvi-app.xyz
          methods:
            - GET
            - POST
            - PUT
            - PATCH
            - DELETE
            - HEAD
            - OPTIONS
          headers:
            - Accept
            - Authorization
            - Content-Type
            - X-CSRFToken
            - X-Requested-With
          exposed_headers:
            - X-RateLimit-Limit
            - X-RateLimit-Remaining
            - X-RateLimit-Reset
            - Retry-After
          credentials: true
          max_age: 3600
